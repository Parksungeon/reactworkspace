import React from 'react'

// 상태(status) :
// - 각각의 컴폰넌트가 가지는 데이터
// - 컴포넌트의 렌더링 결과에 영향을 미침
// - 컴포넌트는 독립적인 상태를 가질 수 있음
// - 생태가 변겨오디면 컴포넌트가 리렌더링 됨
export default function StateComponent() {

    // status 선언 :
    // - userState 함수로 함수를 선언할 수 있음

  return (
    <div>StateComponent</div>
  )
}
